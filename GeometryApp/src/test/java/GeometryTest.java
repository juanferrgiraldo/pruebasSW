package test.java;

import main.shape.CheckShape;
//import org.junit.Test;
//import static org.junit.Assert.*;


public class GeometryTest {

    /* Se  necesita hacer un desarrollo en Netbeans  con las siguientes condiciones :

        Hacer una aplicación que permita :
        Recibir 3 Números  y que indique si es un triángulo
        Equilatero
        Isoceles
        Escaleno

      2. Recibir 4 números  y que indique si es :

        Cuadrado
        Rectángulo
        Trapecio
 */
    CheckShape shape = new CheckShape();

    /* @Test
    public void TestTriangle() {
        boolean result = shape.isTriangle(3);
        boolean expected  = true;
        assertEquals(expected, result);
    }

    @Test
    public void TestSquare() {
        boolean result = shape.isSquare(4);
        boolean expected = true;
        assertEquals(expected, result);
    }*/

}
